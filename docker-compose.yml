services:
  backend:
    build: ./backend
    container_name: faculty-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: faculty-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
